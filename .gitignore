node_modules
.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Claude Code local settings (user-specific)
.claude/settings.local.json

# Generated SF Symbol PNGs synced into the web app's public dir (symbols:sync)
apps/web/public/ios-symbols/
